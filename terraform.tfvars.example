# Example Terraform variables file
# Copy this to terraform.tfvars and fill in your values
# Note: terraform.tfvars is gitignored for security

# AWS Configuration
aws_region = "us-east-1"

# Cluster Configuration
cluster_name = "wikijs-cluster"
environment  = "production"

# Database Configuration (REQUIRED)
db_password = "CHANGE_ME_TO_SECURE_PASSWORD"

# Admin Configuration (REQUIRED)
wikijs_admin_email = "admin@example.com"

# Node Configuration (Optional - defaults are cost-optimized)
# node_instance_type = "t3.small"
# desired_capacity   = 1
# min_capacity       = 1
# max_capacity       = 2

# Database Configuration (Optional - defaults are cost-optimized)
# db_instance_class     = "db.t3.micro"
# db_allocated_storage  = 20
